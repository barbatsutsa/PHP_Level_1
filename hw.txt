<?php
$a = 5; 
$b = '05'; 
var_dump($a == $b);     /* Почему true?  - сравнение по значению, РНР динамически приводит 
строку к числовому значению, получаем результат true*/

var_dump((int)'012345');    /* Почему 12345? - Приведение строки к целому числовому значению*/

var_dump((float)123.0 === (int)123.0);    /* Почему false? - при сравнении по типу и значению, несмотря на одинаковое значение, при явно заданных разных типах получаем результат false*/

var_dump((int)0 === (int)'hello, world');   /* Почему true? – при сравнении по типу и значению т.к. в обоих выражениях явно задан одинаковый тип переменной, а во втором выражении при преобразовании строки в целое число возвращаемое значение 0,  получаем результат true*/

?>


<?php

    $a = 3; 
    $b = 7; 

    echo "До:<br>\$a=$a<br>";
    echo "\$b=$b<hr>";

    $a = $a + $b;
    $b = $a - $b;
    $a = $a - $b;

    echo "После:<br>\$a=$a<br>";
    echo "\$b=$b<hr>";

?>